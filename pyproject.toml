[project]
name = "stat163-2026t1-week2-practice"
version = "0.1.0"
description = "STAT163 Week 2 practice — two files, one table"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy",
    "jupyterlab>=4.0",
]
